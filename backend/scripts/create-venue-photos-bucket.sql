-- This script creates the venue-photos storage bucket in Supabase
-- Run this in Supabase SQL Editor or use the Supabase Dashboard:
-- Storage > Create Bucket > Name: venue-photos, Public: true

-- Note: Storage buckets cannot be created via SQL in Supabase
-- You must create it manually in the Supabase Dashboard:
-- 1. Go to Storage section
-- 2. Click "New bucket"
-- 3. Name: venue-photos
-- 4. Public: true (checked)
-- 5. File size limit: 5MB (optional)
-- 6. Allowed MIME types: image/jpeg, image/png, image/webp (optional)

-- Alternatively, you can create it via the Supabase Management API or use the backend code
-- which will attempt to create it automatically (requires proper permissions)

